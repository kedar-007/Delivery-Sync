// Shared task field limits. A standard cap on description length keeps the UI
// (and stored data) sane — long descriptions also scroll in the detail view.
export const TASK_DESCRIPTION_MAX_LENGTH = 2000;
